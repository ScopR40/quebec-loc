{% extends 'base.html.twig' %}

{% block title %}
   {{ ad.title }}
{% endblock %}

{% block body %}

<div class="adtitle" style="background-image: url({{ad.coverImage}})">
   <div class="container">
      <h1>{{ad.title}}</h1>
      <p>{{ad.introduction}}</p>
      <p class="h2">
         <strong>{{ad.rooms}} chambres</strong> pour <strong>{{ad.price}}&euro;</strong> par nuit
      </p>
      <a href="#" class="btn btn-primary">Réserver !</a>
   </div>
</div>

<div class="container">
   <div class="row">
      <div class="col-md-8 mb-3">
         {{ ad.content | raw}}

         <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
               {% for image in ad.images %} {# pour chaque image qu'il y a dans ad.images #}
              <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}{# indique sur quel index on se trouve #}" {% if loop.first %} class="active" {% endif %}></li> {# si c'est la 1ere fois que je rentre dans la boucle alors je mets active #}
              {% endfor %}
            </ol>
            <div class="carousel-inner">
               {% for image in ad.images %} {# pour chaque image qu'il y a dans ad.images #}
                  <div class="carousel-item {% if loop.first %} active {% endif %}"> {# si c'est la 1ere fois que je rentre dans la boucle alors je mets active #}
                     <img src="{{ image.url }}" class="d-block w-100" alt="...">
                     <div class="carousel-caption d-none d-md-block">
                        <h5>{{ ad.title }}</h5>
                        <p>{{ image.caption }}</p>
                     </div>
                  </div>
               {% endfor %}
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
               <span class="carousel-control-prev-icon" aria-hidden="true"></span>
               <span class="sr-only">Précédente</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
               <span class="carousel-control-next-icon" aria-hidden="true"></span>
               <span class="sr-only">Suivante</span>
            </a>
         </div>
      </div>
      <div class="col">
         <div class="row">
            <div class="col-4">
               <img src="http://placehold.it/64x64" alt="Avatar de Dédé">
            </div>
            <div class="col">
               <h3>Dédé la Bidouille</h3>
               <span class="badge badge-primary">
                  3 annonces
               </span>
            </div>
         </div>
         <p>Lorem ipsum dolor sit amet consectetur, adipisicing elit. Dicta eum laborum blanditiis inventore quidem possimus dolores dignissimos reprehenderit rem quaerat.</p>
         <p>Minus cupiditate repudiandae magnam eligendi consequuntur provident, eaque nihil optio voluptatibus officiis nulla obcaecati suscipit! Molestias alias dolor reprehenderit praesentium!</p>
         <p>Consequuntur quam quisquam in veritatis facilis maxime nostrum? Numquam voluptas vero unde culpa deleniti, dolor corrupti consequatur ea praesentium qui?</p>
      </div>
   </div>
</div>

{% endblock %}